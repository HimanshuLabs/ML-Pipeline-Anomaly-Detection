global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: "project4-anomaly-api"
    metrics_path: "/metrics"
    static_configs:
      - targets:
          - "localhost:8004"
        labels:
          service: "project4-online-anomaly-inference"
          project: "project4"
          environment: "local"
